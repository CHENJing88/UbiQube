<%= form_for(@app, :remote => true) do |f| %>
<%= render 'layouts/error_message', object: f.object %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :nom; placeholder:"Nom..." %>
  </div>
  <div class="actions">
    <%= f.submit "Créer", class: "btn btn-large btn-primary" %>
  </div>
<% end %>
