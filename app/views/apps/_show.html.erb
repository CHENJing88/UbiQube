<%# form_for @app, :url=> {controller=>'apps', :action=> 'show'}, remote:true do |f| %>

<%# form_for @app do |f| %>
  <%#= render 'apps/form' %>
<%# end %>
<% form_for @app do |f| %>
<div class="field">


  <p><%= f.label :description %><%= f.text_field :description %></p>
</div>

<div class="action">
  <%= f.submit %>
</div>
<% end %>

<%= link_to "Delete", mesapps_url(current_user) ,data:{:confirm => 'Vous etês sure delete cet App?'}, :method => :delete %>
