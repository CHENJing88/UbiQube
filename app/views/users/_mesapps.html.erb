<%= @mesapps.each do |a|%>
  <!--div class="card-section" href="/apps/show" data-reveal-id="myModal" -->
  <%= link_to app_path(a.apps),:class => 'close-reveal-modal', :data => { :reveal_id => 'myModal' } do %>
  <li class="custom-card">
    <p><%= a.apps.nom %></p>
    <p><%= a.roles.nom %></p>
  </li>
  <%end%>

<% end%>
<li class="nouvel-card">
  <p>
    Céer une nouvelle App
  </p>
</li>
