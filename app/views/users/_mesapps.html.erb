<%= @mesapps.each do |a|%>
  <!--div class="card-section" href="/apps/show" data-reveal-id="myModal" -->
  <%#= link_to app_path(a.apps.id),:class => 'close-reveal-modal',
  :data => { :reveal_id => 'appModal', :"reveal-ajax" => true } do %>
 <a href="#" data-reveal-id="appModal" >
  <li class="custom-card"  >
    <p><%= a.apps.nom %></p>
    <p><%= a.roles.nom %></p>
  </li>

  <div id="appModal" class="reveal-modal" data-reveal arial-labelledby="modalTitle"
        aria-hidden="true" role="dialog">

        <%= render apps_path(a.apps) %>

  </div>
</a>
<% end%>

<li class="nouvel-card">
  <p>
    Céer une nouvelle App
  </p>
</li>
